---
import Layout from "../layouts/Layout.astro";
---

<Layout title="404 - Página no encontrada" descripcion="Página no encontrada">
    <main
        class="relative h-screen min-h-screen overflow-hidden bg-linear-to-br from-[#020617] via-[#232629] to-black text-white"
    >
        <!-- Background glow -->
        <section class="pointer-events-none absolute inset-0" id="glow-1">
            <div class="absolute -top-40 -left-40 h-96 w-96 rounded-full bg-green-500/20 blur-3xl"></div>
            <div class="absolute right-0 bottom-0 h-96 w-96 rounded-full bg-emerald-400/10 blur-3xl"></div>
        </section>

        <section class="pointer-events-none absolute inset-0" id="glow-2">
            <div class="absolute -top-40 -left-40 h-96 w-96 rounded-full bg-green-500/20 blur-3xl"></div>
            <div class="absolute right-0 bottom-0 h-96 w-96 rounded-full bg-emerald-400/10 blur-3xl"></div>
        </section>

        <section class="pointer-events-none absolute inset-0" id="glow-3">
            <div class="absolute -bottom-40 -left-40 h-96 w-96 rounded-full bg-green-500/20 blur-3xl"></div>
            <div class="absolute right-0 bottom-0 h-96 w-96 rounded-full bg-emerald-400/10 blur-3xl"></div>
        </section>

        <!-- Center -->
        <section class="relative z-10 flex min-h-screen items-center justify-center px-6">
            <div
                class="w-full max-w-xl rounded-3xl border border-green-400/20 bg-white/5 p-10 text-center shadow-2xl backdrop-blur-xl"
            >
                <!-- Logo -->
                <picture
                    class="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-2xl bg-black/60 shadow-inner"
                >
                    <!-- HM SVG -->
                    <img src={"./logo.jpg"} alt="Logo" class="h-full w-full rounded-2xl object-contain" />
                </picture>

                <!-- 404 -->
                <h1
                    class="text-7xl font-extrabold tracking-widest text-green-400 drop-shadow-[0_0_20px_rgba(34,197,94,0.4)] md:text-8xl"
                >
                    404
                </h1>

                <h2 class="mt-4 text-2xl font-semibold">Página no encontrada</h2>

                <p class="mt-4 text-sm leading-relaxed text-white/70">
                    La ruta que intentas acceder no existe o fue eliminada. Posiblemente el sistema bloqueó el acceso 🔐
                </p>

                <!-- Button -->
                <a
                    href="/"
                    class="mt-8 inline-flex items-center justify-center rounded-xl bg-green-500 px-6 py-3 text-sm font-medium text-black shadow-lg shadow-green-500/30 transition hover:scale-105 hover:bg-green-400"
                >
                    Volver al inicio
                </a>
            </div>
        </section>
    </main>

    <script>
        import gsap from "gsap";

        gsap.from("section", {
            opacity: 0,
            y: 40,
            duration: 1,
            ease: "power3.out",
        });

        gsap.from("h1", {
            scale: 0.8,
            opacity: 0,
            delay: 0.2,
            duration: 0.8,
        });

        gsap.from("#glow-1", {
            opacity: 0,
            translateY: 80,
            duration: 0.5,
            ease: "power3.out",
        });

        gsap.from("#glow-2", {
            opacity: 0,
            translateY: 80,
            duration: 0.5,
            ease: "power3.out",
        });
        gsap.from("#glow-3", {
            opacity: 0,
            translateY: 80,
            duration: 1,
            ease: "power3.out",
            stagger: 0.1,
        });
    </script>
</Layout>
